name: Disabled - Using gh-pages deployment

# This workflow is disabled because we use gh-pages branch deployment
# Run 'npm run deploy' to deploy manually

on:
  # No triggers - workflow disabled
  workflow_dispatch:
    inputs:
      note:
        description: 'This workflow is disabled. Use npm run deploy instead.'
        required: false

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Deployment Info
        run: |
          echo "This GitHub Actions workflow is disabled."
          echo "The project uses gh-pages branch deployment instead."
          echo ""
          echo "To deploy:"
          echo "1. Run: npm run build"
          echo "2. Run: npm run deploy" 
          echo ""
          echo "Site URL: https://snarky1980.github.io/email-assistant-v8-fixed/"